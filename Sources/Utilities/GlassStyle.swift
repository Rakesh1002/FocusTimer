// This file is deprecated - use ColorTheme.swift instead
// Keeping for backwards compatibility

import SwiftUI

extension View {
    func glassCard(cornerRadius: CGFloat = 14) -> some View {
        self.card(cornerRadius: cornerRadius)
    }
}

